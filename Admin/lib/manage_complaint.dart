import 'package:flutter/material.dart';

class Managecomplaint extends StatelessWidget {
  const Managecomplaint({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}